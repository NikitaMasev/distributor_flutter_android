name: distributor_flutter_android
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.1.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  rw_git: ^1.0.3
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
